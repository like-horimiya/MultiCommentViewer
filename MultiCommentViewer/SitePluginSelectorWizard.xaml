<Window x:Class="MultiCommentViewer.SitePluginSelectorWizard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="使用するサイトを選択" Height="550" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F0F0F0">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- タイトル -->
        <TextBlock Grid.Row="0" 
                   Text="使用するコメントビューワーサイトを選択してください" 
                   FontSize="16" 
                   FontWeight="Bold"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

        <!-- 説明 -->
        <TextBlock Grid.Row="1" 
                   TextWrapping="Wrap"
                   Margin="0,0,0,15">
            <Run Text="起動を高速化するため、使用するサイトのみを選択してください。"/><LineBreak/>
            <Run Text="後から設定画面で変更できます。" FontWeight="Bold"/>
        </TextBlock>

        <!-- サイト選択リスト -->
        <Border Grid.Row="2" 
                BorderBrush="#CCC" 
                BorderThickness="1" 
                Background="White"
                Margin="0,0,0,15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="SiteList" Margin="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Name}" 
                                      IsChecked="{Binding IsEnabled}"
                                      Margin="0,5"
                                      FontSize="14"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- 選択/解除ボタン -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Left"
                    Margin="0,0,0,15">
            <Button x:Name="SelectAllButton" 
                    Content="全て選択" 
                    Width="100" 
                    Margin="0,0,10,0"
                    Click="SelectAllButton_Click"/>
            <Button x:Name="DeselectAllButton" 
                    Content="全て解除" 
                    Width="100"
                    Click="DeselectAllButton_Click"/>
        </StackPanel>

        <!-- OKキャンセルボタン -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="OkButton" 
                    Content="OK" 
                    Width="100" 
                    Height="30"
                    Margin="0,0,10,0"
                    IsDefault="True"
                    Click="OkButton_Click"/>
            <Button x:Name="CancelButton" 
                    Content="キャンセル" 
                    Width="100" 
                    Height="30"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
